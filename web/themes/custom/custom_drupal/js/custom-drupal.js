/**
 * @file
 * Custom Drupal behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customDrupal = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
